<p style="color: green"><%= notice %></p>

<h1>Promotions</h1>

<%= link_to "New promotion", new_promotion_path %>

<div id="promotions">
  <% @promotions.each do |promotion| %>
    <%= render promotion %>
    <p>
      <%= link_to "Show this promotion", promotion %>
    </p>
  <% end %>
</div>
