<%= form_with(model: person_promotion) do |form| %>
  <% if person_promotion.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(person_promotion.errors.count, "error") %> prohibited this person_promotion from being saved:</h2>

      <ul>
        <% person_promotion.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :promotion_id, style: "display: block" %>
    <%= form.select :promotion_id, options_for_select(Promotion.all.map { |p| [p.name, p.id] }),class: "form-select" %>
  </div>

  <div>
    <%= form.label :person_id, style: "display: block" %>
    <%= form.select :person_id, options_for_select(Person.where(type_person_id: TypePerson.find_by(slug: 'STU')).map { |p| [p.lastname, p.id] }),class: "form-select" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
